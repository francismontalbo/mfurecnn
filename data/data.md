# Data directory

Extract the prepared dataset archive (`data.rar`) into this folder.

Expected usage:
- This directory stores the processed data layout used by the notebooks.
- Keep raw dataset sources and checksums documented externally for traceability.

# Models directory

Extract pretrained model weights archive (`models.rar`) into this folder.

Expected usage:
- Notebook evaluation/testing reads model artifacts from this location.
- Retraining notebooks may overwrite files; create backups or versioned copies first.
